package base;

import org.openqa.selenium.WebDriver;
import java.util.Properties;

public class BaseTest {

    protected WebDriver driver;//to hold the WebDriver instance that will be used across all test classes
    protected Properties prop;//to hold the configuration properties that will be used across all test classes

}
